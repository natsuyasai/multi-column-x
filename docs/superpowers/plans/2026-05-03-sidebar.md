# Sidebar Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** アプリ左端に開閉可能な固定サイドバーを追加し、ツイート入力・カラムジャンプ・カラム追加・アカウント管理ボタンを縦に並べる（Phase 1 はボタン配置のみ、機能はダミー）。

**Architecture:** `position: fixed; left: 0` の Sidebar コンポーネントを新規作成し、メインコンテンツは `padding-left` でオフセット。Tauri 子 WebView の x 座標も `sidebarWidth` 分加算するよう `calculateBounds` を修正。開閉状態は Zustand に `sidebarExpanded: boolean` として保持。

**Tech Stack:** React 19 + TypeScript + SCSS Modules + Zustand（既存スタック）

---

## File Map

| ファイル                                     | 種別     | 担当                                              |
| -------------------------------------------- | -------- | ------------------------------------------------- |
| `src/components/Sidebar/Sidebar.tsx`         | 新規作成 | サイドバーUIコンポーネント                        |
| `src/components/Sidebar/Sidebar.module.scss` | 新規作成 | サイドバースタイル                                |
| `src/store/useAppStore.ts`                   | 修正     | `sidebarExpanded` 状態追加                        |
| `src/hooks/useColumns.ts`                    | 修正     | `calculateBounds` に `sidebarWidth` 追加          |
| `src/App.tsx`                                | 修正     | Sidebar 組み込み、toolbar 削除、padding-left 追加 |
| `src/App.module.scss`                        | 修正     | toolbar スタイル削除、padding-left 対応           |

---

## Task 1: Zustand に sidebarExpanded を追加

**Files:**

- Modify: `src/store/useAppStore.ts`

- [ ] **Step 1: `AppStore` インターフェースに `sidebarExpanded` と `setSidebarExpanded` を追加する**

[src/store/useAppStore.ts](src/store/useAppStore.ts) の `interface AppStore` に以下を追加：

```ts
interface AppStore {
  accounts: Account[];
  columns: Column[];
  globalSettings: GlobalSettings;
  isLoaded: boolean;
  sidebarExpanded: boolean; // ← 追加
  setSidebarExpanded: (v: boolean) => void; // ← 追加
  // ... 既存メソッド
}
```

- [ ] **Step 2: `create` の初期値と実装を追加する**

`useAppStore` の `create` 内に以下を追加（`isLoaded: false,` の直後）：

```ts
sidebarExpanded: false,
setSidebarExpanded: (v) => set({ sidebarExpanded: v }),
```

- [ ] **Step 3: 動作確認（型チェック）**

```powershell
cd C:\Users\fuku\Desktop\twitter-viewer
npm run type-check 2>&1 | head -20
```

型エラーがなければ OK（`type-check` がなければ `npx tsc --noEmit` で代用）。

- [ ] **Step 4: コミット**

```powershell
git add src/store/useAppStore.ts
git commit -m "feat: add sidebarExpanded state to store"
```

---

## Task 2: Sidebar コンポーネントを作成

**Files:**

- Create: `src/components/Sidebar/Sidebar.tsx`
- Create: `src/components/Sidebar/Sidebar.module.scss`

- [ ] **Step 1: `Sidebar.module.scss` を作成する**

```scss
// src/components/Sidebar/Sidebar.module.scss
$sidebar-collapsed-width: 40px;
$sidebar-expanded-width: 70px;
$transition: 200ms ease;

.sidebar {
  position: fixed;
  left: 0;
  top: 0;
  bottom: 0;
  z-index: 100;
  width: $sidebar-collapsed-width;
  background: #0a0a0a;
  border-right: 1px solid #222;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 6px 0;
  gap: 4px;
  transition: width $transition;
  overflow: hidden;

  &.expanded {
    width: $sidebar-expanded-width;
  }
}

.btn {
  width: 28px;
  height: 28px;
  background: none;
  border: none;
  color: #555;
  cursor: pointer;
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 15px;
  flex-shrink: 0;
  padding: 0;
  transition:
    width $transition,
    background $transition;

  &:hover {
    color: #aaa;
    background: #1a1a1a;
  }
}

.btnExpanded {
  width: 58px;
  height: 28px;
  flex-direction: row;
  gap: 5px;
  justify-content: flex-start;
  padding: 0 6px;
  font-size: 12px;

  .icon {
    font-size: 14px;
    flex-shrink: 0;
  }

  .label {
    font-size: 11px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    color: #888;
  }
}

.composeTweetBtn {
  composes: btn;
  background: #1d9bf0;
  color: #fff;

  &:hover {
    background: #1a8cd8;
    color: #fff;
  }
}

.composeTweetBtnExpanded {
  composes: btnExpanded;
  background: #1d9bf0;
  color: #fff;

  .icon,
  .label {
    color: #fff;
  }

  &:hover {
    background: #1a8cd8;
  }
}

.divider {
  width: 24px;
  height: 1px;
  background: #2a2a2a;
  flex-shrink: 0;
  transition: width $transition;

  .expanded & {
    width: 54px;
  }
}

.columnList {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 3px;
  overflow-y: auto;
  overflow-x: hidden;
  flex: 1;
  width: 100%;
  padding: 0 0 2px;
  scrollbar-width: none;

  &::-webkit-scrollbar {
    display: none;
  }
}

.spacer {
  flex: 1;
}

.toggleBtn {
  composes: btn;
  margin-top: 2px;
  color: #444;
  font-size: 13px;

  &:hover {
    color: #888;
    background: #1a1a1a;
  }
}

.toggleBtnExpanded {
  composes: btnExpanded;
  color: #444;

  &:hover {
    color: #888;
    background: #1a1a1a;
  }

  .icon,
  .label {
    color: #444;
  }
}
```

- [ ] **Step 2: `Sidebar.tsx` を作成する**

```tsx
// src/components/Sidebar/Sidebar.tsx
import React from "react";
import type { Column } from "../../types";
import styles from "./Sidebar.module.scss";

interface SidebarProps {
  columns: Column[];
  expanded: boolean;
  onToggleExpand: () => void;
  onAddColumn: () => void;
  onAccountManager: () => void;
  onComposeTweet: () => void;
  onJumpToColumn: (columnId: string) => void;
}

function columnDisplayName(column: Column): string {
  if (column.label) return column.label;
  switch (column.pageType) {
    case "home":
      return "ホーム";
    case "notifications":
      return "通知";
    case "search":
      return column.searchQuery ?? "検索";
    case "list":
      return "リスト";
    case "custom":
      return "カスタム";
  }
}

export const Sidebar: React.FC<SidebarProps> = ({
  columns,
  expanded,
  onToggleExpand,
  onAddColumn,
  onAccountManager,
  onComposeTweet,
  onJumpToColumn,
}) => {
  const sorted = [...columns].sort((a, b) => a.order - b.order);

  return (
    <div
      className={`${styles.sidebar}${expanded ? ` ${styles.expanded}` : ""}`}
    >
      {/* ツイート入力ボタン */}
      {expanded ? (
        <button
          className={`${styles.composeTweetBtn} ${styles.composeTweetBtnExpanded}`}
          onClick={onComposeTweet}
          title="ツイートを作成"
        >
          <span className={styles.icon}>✏️</span>
          <span className={styles.label}>ツイート</span>
        </button>
      ) : (
        <button
          className={styles.composeTweetBtn}
          onClick={onComposeTweet}
          title="ツイートを作成"
        >
          ✏️
        </button>
      )}

      <div className={styles.divider} />

      {/* カラムジャンプボタン群 */}
      <div className={styles.columnList}>
        {sorted.map((col) =>
          expanded ? (
            <button
              key={col.id}
              className={`${styles.btn} ${styles.btnExpanded}`}
              onClick={() => onJumpToColumn(col.id)}
              title={columnDisplayName(col)}
            >
              <span className={styles.icon}>📋</span>
              <span className={styles.label}>{columnDisplayName(col)}</span>
            </button>
          ) : (
            <button
              key={col.id}
              className={styles.btn}
              onClick={() => onJumpToColumn(col.id)}
              title={columnDisplayName(col)}
            >
              📋
            </button>
          ),
        )}
      </div>

      {/* カラム追加 */}
      {expanded ? (
        <button
          className={`${styles.btn} ${styles.btnExpanded}`}
          onClick={onAddColumn}
          title="カラムを追加"
        >
          <span className={styles.icon}>＋</span>
          <span className={styles.label}>カラム追加</span>
        </button>
      ) : (
        <button
          className={styles.btn}
          onClick={onAddColumn}
          title="カラムを追加"
        >
          ＋
        </button>
      )}

      {/* アカウント管理 */}
      {expanded ? (
        <button
          className={`${styles.btn} ${styles.btnExpanded}`}
          onClick={onAccountManager}
          title="アカウント管理"
        >
          <span className={styles.icon}>👤</span>
          <span className={styles.label}>アカウント</span>
        </button>
      ) : (
        <button
          className={styles.btn}
          onClick={onAccountManager}
          title="アカウント管理"
        >
          👤
        </button>
      )}

      {/* 開閉ボタン */}
      {expanded ? (
        <button
          className={`${styles.toggleBtn} ${styles.toggleBtnExpanded}`}
          onClick={onToggleExpand}
          title="サイドバーを閉じる"
        >
          <span className={styles.icon}>«</span>
          <span className={styles.label}>閉じる</span>
        </button>
      ) : (
        <button
          className={styles.toggleBtn}
          onClick={onToggleExpand}
          title="サイドバーを開く"
        >
          »
        </button>
      )}
    </div>
  );
};
```

- [ ] **Step 3: 型チェック**

```powershell
npx tsc --noEmit 2>&1 | head -30
```

エラーなければ OK。

- [ ] **Step 4: コミット**

```powershell
git add src/components/Sidebar/Sidebar.tsx src/components/Sidebar/Sidebar.module.scss
git commit -m "feat: add Sidebar component (layout only)"
```

---

## Task 3: useColumns の calculateBounds に sidebarWidth を追加

**Files:**

- Modify: `src/hooks/useColumns.ts`

- [ ] **Step 1: `calculateBounds` のシグネチャに `sidebarWidth` を追加する**

[src/hooks/useColumns.ts:17-34](src/hooks/useColumns.ts#L17-L34) の `calculateBounds` を以下に置き換える：

```ts
const calculateBounds = useCallback(
  (
    columnIndex: number,
    allColumns: Column[],
    _containerWidth: number,
    containerHeight: number,
    scrollLeft: number = 0,
    sidebarWidth: number = 0,
  ) => {
    let x = sidebarWidth;
    for (let i = 0; i < columnIndex; i++) {
      x += allColumns[i].width;
    }
    return {
      x: x - scrollLeft,
      y: HEADER_HEIGHT,
      width: allColumns[columnIndex].width,
      height: containerHeight - HEADER_HEIGHT - SCROLLBAR_HEIGHT,
    };
  },
  [],
);
```

- [ ] **Step 2: `restoreColumns` に sidebarWidth を渡す**

[src/hooks/useColumns.ts:37-60](src/hooks/useColumns.ts#L37-L60) の `restoreColumns` 内、`calculateBounds` 呼び出し箇所を修正：

```ts
const restoreColumns = useCallback(async () => {
  if (!containerRef.current) return;
  const containerWidth = containerRef.current.clientWidth;
  const containerHeight = containerRef.current.clientHeight;
  const scrollLeft = scrollRef.current?.scrollLeft ?? 0;
  const {
    columns: currentColumns,
    accounts: currentAccounts,
    sidebarExpanded,
  } = useAppStore.getState();
  const sidebarWidth = sidebarExpanded ? 70 : 40;
  const sortedColumns = [...currentColumns].sort((a, b) => a.order - b.order);

  for (let i = 0; i < sortedColumns.length; i++) {
    const column = sortedColumns[i];
    const account = currentAccounts.find((a) => a.id === column.accountId);
    if (!account) continue;

    const bounds = calculateBounds(
      i,
      sortedColumns,
      containerWidth,
      containerHeight,
      scrollLeft,
      sidebarWidth,
    );
    await invoke("create_column_webview", {
      args: {
        column,
        dataDirectory: account.dataDirectory,
        ...bounds,
      },
    }).catch(console.error);
  }
}, [calculateBounds]);
```

- [ ] **Step 3: `recalculateAllBounds` に sidebarWidth を渡す**

[src/hooks/useColumns.ts:63-77](src/hooks/useColumns.ts#L63-L77) の `recalculateAllBounds` を修正：

```ts
const recalculateAllBounds = useCallback(async () => {
  if (!containerRef.current) return;
  const containerHeight = containerRef.current.clientHeight;
  const containerWidth = containerRef.current.clientWidth;
  const scrollLeft = scrollRef.current?.scrollLeft ?? 0;
  const { columns: currentColumns, sidebarExpanded } = useAppStore.getState();
  const sidebarWidth = sidebarExpanded ? 70 : 40;
  const sortedColumns = [...currentColumns].sort((a, b) => a.order - b.order);

  for (let i = 0; i < sortedColumns.length; i++) {
    const bounds = calculateBounds(
      i,
      sortedColumns,
      containerWidth,
      containerHeight,
      scrollLeft,
      sidebarWidth,
    );
    await invoke("resize_column_webview", {
      bounds: { columnId: sortedColumns[i].id, ...bounds },
    }).catch(console.error);
  }
}, [calculateBounds]);
```

- [ ] **Step 4: `handleAddColumn` に sidebarWidth を渡す**

[src/hooks/useColumns.ts:80-99](src/hooks/useColumns.ts#L80-L99) の `handleAddColumn` を修正：

```ts
const handleAddColumn = useCallback(
  async (column: Column) => {
    const account = accounts.find((a) => a.id === column.accountId);
    if (!account || !containerRef.current) return;

    const orderedColumns = [...columns, { ...column, order: columns.length }];
    const containerHeight = containerRef.current.clientHeight;
    const containerWidth = containerRef.current.clientWidth;
    const scrollLeft = scrollRef.current?.scrollLeft ?? 0;
    const { sidebarExpanded } = useAppStore.getState();
    const sidebarWidth = sidebarExpanded ? 70 : 40;
    const bounds = calculateBounds(
      orderedColumns.length - 1,
      orderedColumns,
      containerWidth,
      containerHeight,
      scrollLeft,
      sidebarWidth,
    );

    await invoke("create_column_webview", {
      args: {
        column: { ...column, order: columns.length },
        dataDirectory: account.dataDirectory,
        ...bounds,
      },
    });

    addColumn({ ...column, order: columns.length });
  },
  [columns, accounts, addColumn, calculateBounds],
);
```

- [ ] **Step 5: 型チェック**

```powershell
npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 6: コミット**

```powershell
git add src/hooks/useColumns.ts
git commit -m "feat: pass sidebarWidth to calculateBounds"
```

---

## Task 4: App.tsx に Sidebar を組み込み、toolbar を削除

**Files:**

- Modify: `src/App.tsx`
- Modify: `src/App.module.scss`

- [ ] **Step 1: `App.tsx` に Sidebar の import と sidebarExpanded を追加する**

`App.tsx` 冒頭の import ブロックに追加：

```tsx
import { Sidebar } from "./components/Sidebar/Sidebar";
```

`useAppStore` の destructure に `sidebarExpanded` / `setSidebarExpanded` を追加：

```tsx
const {
  loadSettings,
  isLoaded,
  accounts,
  sidebarExpanded,
  setSidebarExpanded,
} = useAppStore();
```

- [ ] **Step 2: `sidebarWidth` の useMemo を追加し、`toolbarRef` と古い `scrollbarWidth` を削除する**

既存の `toolbarRef` 定義と `scrollbarWidth` useMemo を以下に置き換える：

```tsx
const sidebarWidth = sidebarExpanded ? 70 : 40;

const scrollbarWidth = useMemo(() => {
  const columnsWidth = columns.reduce((sum, c) => sum + c.width, 0);
  return columnsWidth + sidebarWidth;
}, [columns, sidebarWidth]);
```

（`toolbarRef` は削除する）

- [ ] **Step 3: サイドバー開閉ハンドラを追加する**

`handleReload` の前に追加：

```tsx
const handleToggleSidebar = useCallback(() => {
  setSidebarExpanded(!sidebarExpanded);
  // サイドバー幅変更後に WebView 位置を更新（CSS transition 完了後）
  setTimeout(() => recalculateAllBounds(), 220);
}, [sidebarExpanded, setSidebarExpanded, recalculateAllBounds]);
```

- [ ] **Step 4: JSX を修正する — Sidebar 追加・toolbar 削除・padding-left 追加**

`return (...)` 内を以下に変更：

```tsx
return (
  <div
    className={styles.app}
    ref={containerRef}
    style={{ paddingLeft: sidebarWidth }}
  >
    <Sidebar
      columns={columns}
      expanded={sidebarExpanded}
      onToggleExpand={handleToggleSidebar}
      onAddColumn={() => setShowAddColumn(true)}
      onAccountManager={() => setShowAccountManager(true)}
      onComposeTweet={() => {}}
      onJumpToColumn={() => {}}
    />

    <div
      className={styles.headerRow}
      onWheel={(e) => {
        const el = scrollbarRef.current;
        if (!el) return;
        el.scrollLeft += e.deltaY !== 0 ? e.deltaY : e.deltaX;
      }}
    >
      <div
        className={styles.columnHeaders}
        ref={scrollRef}
        onScroll={handleHeaderScroll}
      >
        {(() => {
          const sorted = columns.slice().sort((a, b) => a.order - b.order);
          return sorted.map((column, idx) => {
            const account = accounts.find((a) => a.id === column.accountId);
            if (!account) return null;
            return (
              <div
                key={column.id}
                style={{ width: column.width, flexShrink: 0 }}
              >
                <ColumnHeader
                  column={column}
                  account={account}
                  onReload={handleReload}
                  onMoveLeft={(id) => handleMoveColumn(id, "left")}
                  onMoveRight={(id) => handleMoveColumn(id, "right")}
                  onSettings={setSettingsColumnId}
                  onClose={handleRemoveColumn}
                  isFirst={idx === 0}
                  isLast={idx === sorted.length - 1}
                />
              </div>
            );
          });
        })()}
      </div>
    </div>

    <div className={styles.webviewArea} />

    <div
      className={styles.bottomScrollbar}
      ref={scrollbarRef}
      onScroll={handleScrollbarScroll}
    >
      <div
        className={styles.bottomScrollbarInner}
        style={{ width: scrollbarWidth }}
      />
    </div>

    {showAddColumn && accounts.length > 0 && (
      <AddColumnDialog
        accounts={accounts}
        onAdd={(column) => {
          handleAddColumn(column);
          setShowAddColumn(false);
        }}
        onCancel={() => setShowAddColumn(false)}
      />
    )}

    {showAddColumn && accounts.length === 0 && (
      <div className={styles.noAccountsPrompt}>
        <p>先にアカウントを追加してください</p>
        <button
          onClick={() => {
            setShowAddColumn(false);
            setShowAccountManager(true);
          }}
        >
          アカウント管理を開く
        </button>
      </div>
    )}

    {showAccountManager && (
      <AccountManager
        accounts={accounts}
        onAddAccount={startAddAccount}
        onRemoveAccount={removeAccount}
        onClose={() => setShowAccountManager(false)}
      />
    )}

    {settingsColumnId &&
      (() => {
        const col = columns.find((c) => c.id === settingsColumnId);
        return col ? (
          <SettingsPanel
            column={col}
            onApply={handleApplySettings}
            onClose={() => setSettingsColumnId(null)}
          />
        ) : null;
      })()}
  </div>
);
```

- [ ] **Step 5: `App.module.scss` の toolbar スタイルを削除する**

[src/App.module.scss:50-74](src/App.module.scss#L50-L74) の `.toolbar` と `.toolbarBtn` ブロックをまるごと削除する。

- [ ] **Step 6: 型チェック**

```powershell
npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 7: コミット**

```powershell
git add src/App.tsx src/App.module.scss
git commit -m "feat: integrate Sidebar into App, remove toolbar"
```

---

## Task 5: 動作確認

- [ ] **Step 1: アプリを起動する**

```powershell
$env:PATH += ";$env:USERPROFILE\.cargo\bin"
npm run tauri:dev
```

- [ ] **Step 2: 以下を目視確認する**

1. アプリ左端に幅 40px のサイドバーが表示される
2. カラムの WebView がサイドバーと重ならず右にオフセットされている
3. 左右スクロール（底部スクロールバー）してもサイドバーが左端に固定されている
4. サイドバー下端の `»` ボタンを押すと幅 70px に展開し、ラベルが表示される
5. `«` ボタンを押すと幅 40px に戻り、WebView 位置も追従する
6. `＋` ボタンでカラム追加ダイアログが開く
7. `👤` ボタンでアカウント管理が開く

- [ ] **Step 3: 問題があれば修正してコミット**

```powershell
git add -p
git commit -m "fix: sidebar layout adjustments"
```
